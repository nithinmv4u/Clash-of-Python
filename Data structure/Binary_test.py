search(value):
    if data in root node:
        result = True
        exit
    else:
        if left node:
                go to left node:
                    check for data through recursion to search
        if right node:
            go to right node:
                check for data through recursion to search
                 
        